// 배열은 일반적으로 선언할 때 배열의 크기를 알려준다. 순서대로 메모리를 할당한다. 하지만 데이터 삽입, 삭제의 성능은 좋지 않다.

// 처음부터 너무 많은 메모리를 할당해놓으면 배열 하나가 메모리를 엄청많이 차지한다. 낭비되는 공간이 생긴다.

//자바스크립트는 배열은 상황에 따라 연속적, 불연속적으로 메모리 할당. 대부분 불연속적으로 할당한다. 사용자에게는 배열인 것 처럼 보인다.
